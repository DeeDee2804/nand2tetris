/**
 * Math utilities.
 */
class MathUtils {
  /**
   * Provide the modulo operation missing from Jack.
   *
   * @param a - the dividend
   * @param b - the divisor
   *
   * @return the remainder of a / b
   */
  function int mod(int a, int b) {
    return (a - ((a / b) * b));
  }
}
